#ifndef CONFIG_HPP_
#define CONFIG_HPP_

#include "Macro.hpp"

#define VERSION_MAJOR ${Warctic_VERSION_MAJOR}
#define VERSION_MINOR ${Warctic_VERSION_MINOR}
#define VERSION_PATCH ${Warctic_VERSION_PATCH}
#define VERSION_SUFFIX "${Warctic_VERSION_SUFFIX}"
#define VERSION_PLATFORM "${Warctic_VERSION_PLATFORM}"

#define VERSION_STRING STR(VERSION_MAJOR) "." STR(VERSION_MINOR) "." STR(VERSION_PATCH) "-" VERSION_SUFFIX " " VERSION_PLATFORM



//Paths
#define PATH_PLUGINS "${OGRE_PLUGIN_PATH}/"

#endif
